name: Automatic Code Quality Review
on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - "**.qml"
permissions:
  contents: read
  pull-requests: write
jobs:
  code-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Automatic Code Quality
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BASE_REF: ${{ github.base_ref }}
          HEAD_REF: ${{ github.head_ref }}
        run: |
          git fetch origin "$BASE_REF"

          diff_changes=$(git diff origin/$BASE_REF...HEAD |
            awk '/^diff --git.*\.qml /{found=1} /^diff --git/{if(!/\.qml /) found=0} found{print}' |
            grep -vE "^(index|\+\+\+|-)") || echo "No qml file changed"

          if [ -z "$diff_changes" ]; then
            echo "No diff changes found!"
            echo "The base ref checking: $BASE_REF"
            echo "The head ref checking: $HEAD_REF"
            exit 0
          fi

          comment_body=$(printf -- '## Automatic Code Quality Review')

          issues=0
          line_index=0

          l_priority="[(L)]($PR_NUMBER 'Low priority, it is okay for this to remain unfixed before merging')"
          h_priority="[(H)]($PR_NUMBER 'High priority, this needs to be fixed for the PR to be merged')"

          function print_line() {
            local priority=$1
            local index=$2
            local comment=$3
            local code_line=$4
            printf -- '\n- %s Line %d: %s\n```qml\n%s\n```' "$priority" "$index" "$comment" "$code_line"
          }

          function print_missing_property() {
            local priority=$1
            local property=$2
            local code_line=$3
            printf -- '\n- %s Missing required property: %s. For example:\n```qml\n%s\n```' "$priority" "$property" "$code_line"
          }

          title_added=false
          file_title=""
          function check_file_title() {
            if [ "$title_added" = false ]; then
              title_added=true
              comment_body+="$file_title"
            fi
          }

          # Required Properties in Components
          inBarWidget=false
          hasPluginApi=false
          hasScreen=false
          hasWidgetId=false
          hasSection=false
          hasSectionWidgetIndex=false
          hasSectionWidgetsCount=false

          function reset_required_properties() {
            inBarWidget=false
            hasPluginApi=false
            hasScreen=false
            hasWidgetId=false
            hasSection=false
            hasSectionWidgetIndex=false
            hasSectionWidgetsCount=false
          }


          while IFS= read -r line; do
            if echo "$line" | grep -E "^diff --git" >/dev/null; then
              if [ "$inBarWidget" = true ]; then
                if [ "$hasPluginApi" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'pluginApi' 'property var pluginApi: null')
                fi
                if [ "$hasScreen" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'screen' 'property ShellScreen screen')
                fi
                if [ "$hasWidgetId" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'widgetId' 'property string widgetId: ""')
                fi
                if [ "$hasSection" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'section' 'property string section: ""')
                fi
                if [ "$hasSectionWidgetIndex" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'sectionWidgetIndex' 'property int sectionWidgetIndex: -1')
                fi
                if [ "$hasSectionWidgetsCount" = false ]; then
                  comment_body+=$(print_missing_property "$h_priority" 'sectionWidgetsCount' 'property int sectionWidgetsCount: 0')
                fi
              fi

              reset_required_properties

              title_added=false
              file_title=$(printf -- '\n### File: %s' "$(echo "$line" | sed 's/diff --git a\/\(.*\.qml\) .*/\1/g')")
              echo "Checking file: $file_title"

              if [[ "$file_title" == *BarWidget.qml ]]; then
                inBarWidget=true
              fi
            elif echo "$line" | grep -E "^@@" >/dev/null; then
              line_index=$(echo "$line" | grep -oE "\+[0-9]+" | grep -oE "[0-9]+")
              echo "Current line index reset: $line_index"
            else
              if echo "$line" | grep -E "(border(\.width)?|spacing|pointSize|radius|margin(s)?): [0-9]+" >/dev/null; then
                check_file_title
                comment_body+=$(print_line "$h_priority" "$line_index" 'Do not use hardcoded values, always prefer to use the `Style` singleton instead' "$line")
                issues=$((issues + 1))
              fi
              if echo "$line" | grep -E "console\.log" >/dev/null; then
                check_file_title
                comment_body+=$(print_line "$h_priority" "$line_index" 'Do not use `console.log`, always prefer to use the `Logger` singleton instead' "$line")
                issues=$((issues + 1))
              fi
              if echo "$line" | grep -E "^(Text|Button|Checkbox|Switch) *\{" >/dev/null; then
                check_file_title
                comment_body+=$(print_line "$h_priority" "$line_index" 'Always prefer to use noctalia-widgets instead of qml widgets to keep it consistent with the shell. For example instead of `Text`, use `NText`' "$line")
                issues=$((issues + 1))
              fi
              if echo "$line" | grep -E "applyUiScale: *false" >/dev/null; then
                if [[ "$file_title" != *BarWidget.qml ]]; then
                  check_file_title
                  comment_body+=$(print_line "$l_priority" "$line_index" 'The `applyUiScale: false` would make it so that the component does not support ui scaling. Always check if this is the correct behaviour you want when changing the ui scale!' "$line")
                  issues=$((issues + 1))
                fi
              fi
              if echo "$line" | grep -E "pluginApi\??\.tr\((.)*\) *(\?\?|\|\|)" >/dev/null; then
                check_file_title
                comment_body+=$(print_line "$l_priority" "$line_index" 'When it comes to translations there is no need for fallback values. From: `pluginApi?.tr("example") || "value"`. To: `pluginApi?.tr("example")`' "$line")
                issues=$((issues + 1))
              fi

              # Check for required properties
              if [ "$inBarWidget" = true ]; then
                if echo "$line" | grep -E 'property var pluginApi: null' >/dev/null; then
                  hasPluginApi=true
                fi
                if echo "$line" | grep -E 'property ShellScreen screen' >/dev/null; then
                  hasScreen=true
                fi
                if echo "$line" | grep -E 'property string widgetId: ""' >/dev/null; then
                  hasWidgetId=true
                fi
                if echo "$line" | grep -E 'property string section: ""' >/dev/null; then
                  hasSection=true
                fi
                if echo "$line" | grep -E 'property int sectionWidgetIndex: -1' >/dev/null; then
                  hasSectionWidgetIndex=true
                fi
                if echo "$line" | grep -E 'property int sectionWidgetsCount: 0' >/dev/null; then
                  hasSectionWidgetsCount=true
                fi
              fi

              line_index=$((line_index + 1))
            fi
          done <<<"$diff_changes"

          if [[ "$issues" -ne 0 ]]; then
            gh pr comment "$PR_NUMBER" --body "$comment_body"
          fi
